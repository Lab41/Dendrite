'use strict';

/* jasmine specs for directives go here */

describe('directives', function() {
  beforeEach(module('dendrite.directives'));

  it('should ....', inject(function() {
    //spec body
  }));
});
